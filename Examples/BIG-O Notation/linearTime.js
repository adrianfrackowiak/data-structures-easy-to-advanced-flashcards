// Example 1
let i = 0;
while (i < n) {
  i += 1;
} // O(n) => Linear Time
// f(n) = n
// O(f(n)) = O(n)

// Example 2
let j = 0;
while (j < n) {
  j += 3;
} // O(n) => Linear Time
// f(n) = n/3
// O(f(n)) = O(n)
